{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Motifini",
        "description": "Enter the address of the Motifini webserver. Enable it in motifini.conf under [webserver] first.",
        "data": {
          "host": "[%key:common::config_flow::data::host%]",
          "port": "[%key:common::config_flow::data::port%]",
          "path_prefix": "Path prefix"
        },
        "data_description": {
          "host": "Hostname or IP of the machine running Motifini.",
          "port": "Motifini webserver port (default 8765).",
          "path_prefix": "Only when behind a reverse proxy path, e.g. motifini. Usually blank."
        }
      }
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    }
  }
}
